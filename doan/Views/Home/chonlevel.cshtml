@model List<doan.Models.Level>

@{
    ViewData["Title"] = "Chọn Cấp Độ";
}

<head>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/chonlevel.css" asp-append-version="true" />
</head>

<body class="bg-gray-100">
    <div class="container mx-auto p-5"> 
        <!-- Thanh điều hướng -->
        <div class="flex flex-wrap items-center justify-between bg-white p-4 rounded-lg shadow-md">
            <a href="/Home/Index" class="text-blue-600 font-bold text-lg">🏠 Trang chủ</a>

            <div class="relative w-1/2">
                <input type="text" id="searchInput" class="w-full p-2 pl-3 pr-10 border rounded-lg shadow-sm focus:ring focus:ring-blue-300" placeholder="Tìm kiếm bài học...">
                <i class="fas fa-search absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                <i class="fas fa-times absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-400 cursor-pointer hover:text-black" onclick="clearSearch()"></i>
            </div>
        </div>

        <!-- Nút chọn Level -->
        <div class="flex space-x-4 mt-5">
            <button onclick="changeLevel('N5')" class="level-btn bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-700">N5</button>
            <button onclick="changeLevel('N4')" class="level-btn bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-700">N4</button>
            <button onclick="changeLevel('N3')" class="level-btn bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-700">N3</button>
        </div>

        <!-- Danh sách bài học -->
        @foreach (var level in Model)
        {
            <div class="mt-6">
                <h2 class="text-xl font-bold text-gray-800">@level.Name - Danh sách bài học</h2>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mt-3">
                    @foreach (var lesson in level.Lessons)
                    {
                        <div class="grid-item level-@level.Name relative bg-white p-4 rounded-lg shadow-md hidden" data-lesson-id="@lesson.Id">
                            <img src="https://dongphucvina.vn/wp-content/uploads/2023/05/hutech-logo.jpeg" alt="@lesson.Name" class="w-full rounded-lg">
                            <h3 class="lesson-title text-lg font-bold mt-2">@lesson.Name</h3>
                            <p class="text-gray-600">Bài học trong cấp độ @level.Name</p>
                            <a asp-controller="Lesson" asp-action="Details" asp-route-id="@lesson.Id" class="block bg-blue-500 text-white px-4 py-2 mt-2 text-center rounded-lg hover:bg-blue-700">Học ngay</a>
                        </div>
                    }
                    </div>
                </div>
            
        }
    </div>

    <!-- Script xử lý logic -->
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            // Lấy cấp độ đã chọn từ URL hoặc từ localStorage
            let params = new URLSearchParams(window.location.search);
            let level = params.get("level") || localStorage.getItem("selectedLevel") || "N5";
            changeLevel(level);

            // Thêm sự kiện tìm kiếm
            document.getElementById("searchInput").addEventListener("input", function () {
                let keyword = this.value.toLowerCase();
                document.querySelectorAll(".lesson-title").forEach(title => {
                    let lessonItem = title.closest(".grid-item");
                    lessonItem.style.display = title.textContent.toLowerCase().includes(keyword) ? "block" : "none";
                });
            });
        });

        // Hàm thay đổi cấp độ bài học
        function changeLevel(level) {
            history.pushState({}, "", "?level=" + level);  // Cập nhật URL
            localStorage.setItem("selectedLevel", level);  // Lưu cấp độ vào localStorage

            // Ẩn tất cả bài học
            document.querySelectorAll(".grid-item").forEach(el => el.style.display = "none");

            // Hiển thị bài học của Level được chọn
            document.querySelectorAll(".level-" + level).forEach(el => el.style.display = "block");

            // Cập nhật trạng thái nút chọn cấp độ
            document.querySelectorAll(".level-btn").forEach(btn => btn.classList.remove("bg-blue-700"));
            document.querySelector(`button[onclick="changeLevel('${level}')"]`).classList.add("bg-blue-700");
        }

        // Hàm xóa từ khóa tìm kiếm và hiển thị lại bài học của cấp độ đã chọn
        function clearSearch() {
            document.getElementById("searchInput").value = "";
            changeLevel(localStorage.getItem("selectedLevel") || "N5");
        }
    </script>

    <!-- CSS -->
    <style>
        .grid-item:hover {
            transform: scale(1.05);
            transition: 0.3s;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
        }

        .level-btn {
            transition: 0.3s;
        }

            .level-btn:hover {
                transform: scale(1.1);
            }
    </style>
</body>
